<% if book.favotited_by?(current_user) %>
  <p>
    <%= link_to book_favorites_path(book), mothod: :delete do %>
    ❤<%= book.favorites.count %> いいね
    <% end %>
  </p>
<% else %>
  <p>
    <%= link_to book_favorites_path(book), mothod: :post do %>
    ♡<%= book.favorites.count %> いいね
    <% end %>
  </p>
<% end %>